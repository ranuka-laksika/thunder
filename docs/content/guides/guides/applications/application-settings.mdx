---
title: Application Settings
sidebar_position: 2
persona: iam
description: Configure authentication flows, registration, tokens, customization, and OAuth 2.0 settings for Thunder applications.
---

# Application Settings

This guide explains how to configure an application from the Thunder Console application details page.

## Find Your Client ID

The **Application ID** and **Client ID** are shown on the application's General tab under **Quick Copy**. Use the copy button to grab them quickly.

- **Application ID** - the internal Thunder identifier for this application.
- **Client ID** - the OAuth 2.0 identifier your SDK or client configuration needs.

## Control Who Can Sign In

Under **Access** on the General tab, you can restrict which users can authenticate with this application.

| Setting | Description |
|---------|-------------|
| **Allowed User Types** | Only users of the selected types can sign in. If none are selected, all user types are allowed. |
| **Application URL** | The homepage URL of your application. |
| **Authorized Redirect URIs** | The URLs Thunder sends users back to after authentication. Register every URI your application uses.

## Use Wildcard Redirect URIs

Thunder supports wildcard patterns in the path component of registered redirect URIs. This lets you register a single pattern that covers a range of valid callback paths, rather than listing every exact URI.

:::note
Wildcard redirect URI support is disabled by default. A Thunder administrator must set `oauth.allow_wildcard_redirect_uri: true` in `deployment.yaml` before you can register wildcard patterns. See [Configuration](/docs/next/guides/getting-started/configuration) for details.
:::

### Wildcard Syntax

| Wildcard | Matches | Example pattern | Matches | Does not match |
|----------|---------|-----------------|---------|----------------|
| `*` | Exactly one path segment (no slashes) | `https://example.com/cb/*` | `https://example.com/cb/v1` | `https://example.com/cb/v1/extra` |
| `**` | Zero or more path segments | `https://example.com/app/**/cb` | `https://example.com/app/cb`, `https://example.com/app/tenant/region/cb` | `https://example.com/app` (missing trailing `cb`) |

### Wildcard Placement Rules

Wildcards are only permitted in the **path** component of a URI.

| URI Component | Wildcards Allowed |
|---------------|-------------------|
| Scheme (e.g., `https`) | No |
| Host / domain (e.g., `example.com`) | No |
| Path (e.g., `/callback/*`) | Yes |
| Query string (e.g., `?foo=bar`) | No |
| Fragment | Not allowed in redirect URIs |

Only `*` and `**` are supported. Full regular expression syntax (e.g., `[a-z]+`) is rejected.

### Matching Rules at Authorization Time

When Thunder receives an authorization request, it evaluates the incoming `redirect_uri` against each registered URI in registration order. The first match wins.

- **Scheme and host** must match exactly (case-insensitive per RFC 3986).
- **Query string** must match exactly. A mismatch in query parameters causes the request to be rejected.
- **Path wildcards** are expanded as described above.
- If `redirect_uri` is omitted from the request and the single registered URI contains a wildcard, the request is rejected — a wildcard pattern cannot serve as a concrete redirect target.

### Token Request Behavior

The `redirect_uri` in a token request is always validated with exact matching per [RFC 6749 §4.1.3](https://www.rfc-editor.org/rfc/rfc6749#section-4.1.3). Wildcard expansion does not apply. The value must exactly match the `redirect_uri` sent in the original authorization request.

### Deep Link Support

Wildcard patterns work for deep link URIs used in mobile applications. Wildcards are permitted only in the path, not in the scheme.

```
# Valid deep link pattern
myapp://callback/*

# Invalid — wildcard in scheme
my*app://callback
```

## Configure Sign-In and Registration Flows

On the **Flows** tab, choose the flows that drive sign-in and sign-up for this application.

**Authentication Flow** - select the flow users follow to sign in. You can assign a custom flow built with the Flow Designer or use the deployment default. Use the **open the flow builder** link to edit the selected flow directly.

**Registration Flow** - toggle the switch to enable or disable self-registration. When enabled, select a flow that defines what information users must provide to create an account.

## Customize the Appearance

On the **Customization** tab, control how your application looks and what legal links users see.

| Setting | Description |
|---------|-------------|
| **Theme** | The visual theme applied to authentication pages. Select **Default Theme** to use the shared deployment theme, or pick an application-specific theme. |
| **Terms of Service URI** | Displayed to users during consent, sign-in, sign-up, or recovery flows. |
| **Privacy Policy URI** | Displayed to users during consent, sign-in, sign-up, or recovery flows. |
| **Contacts** | Administrator email addresses for this application. Separate multiple addresses with commas. |

## Configure What Goes Into Tokens

On the **Token** tab, choose which user attributes each token carries and how long tokens stay valid.

**Access Token** - select the user attributes to include in the access token payload. Default attributes (`sub`, `iss`, `exp`, and others) are always included and cannot be removed. Set **Token Validity** in seconds (for example, `3600` for one hour).

**ID Token** - select the user attributes to include. Only attributes covered by the requested scopes are returned. Set **Token Validity** in seconds. The **Scopes** field shows the OIDC scopes configured for this application (for example, `openid`, `profile`, `email`).

**User Info Attributes** - configure the attributes returned by the `/userinfo` endpoint. Enable **Use same attributes as ID Token** to mirror your ID token selection, or define a separate set.

## Rotate the Client Secret

If you need to invalidate the current client secret, open the General tab and click **Regenerate Client Secret** in the **Danger Zone**.

:::warning
The current client secret is immediately invalidated. Any running clients using the old secret will lose access. Update your application configuration right away.
:::

## Review OAuth 2.0 Configuration

The **Advanced Settings** tab shows the OAuth 2.0 settings configured at creation. These are read-only.

| Setting | Description |
|---------|-------------|
| **Grant Types** | The OAuth 2.0 flows this application can use (for example, `authorization_code`, `refresh_token`, `client_credentials`). |
| **Response Types** | The response types the application can request (for example, `code`). |
| **Public Client** | Whether this is a public client that cannot securely store a client secret. |
| **PKCE Required** | Whether the application must send a `code_challenge` in every authorization request. Always `Yes` for public clients. |

You can also attach a **Certificate** for client authentication or token encryption.

| Certificate Type | Description |
|-----------------|-------------|
| `None` | No certificate configured. |
| `JWKS` | Provide the JSON Web Key Set (JWKS) inline. |
| `JWKS_URI` | Provide the URL of the application's JWKS endpoint. Thunder fetches the public keys to verify signed requests. |

## Related Guides

- [Manage Applications](../applications/manage-applications) - Create, update, and delete applications
- [Flows](/docs/next/guides/guides/flows/what-is-flows) - Build and assign custom authentication and registration flows
