---
title: Users
sidebar_position: 2
persona: iam
description: Create and manage users, including profile attributes and group membership.
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Manage Users

A user in <ProductName /> represents an account that can authenticate with your applications. Each user has:

- A unique **ID** generated by <ProductName /> on account creation.
- A [user type](./user-types) (for example, `customer` or `employee`) that determines the profile attributes, validation rules, and registration settings for that user.
- A set of attributes that match the schema defined by their user type.
- An [organization unit](../organization-units) that defines the user's place in the organizational hierarchy.

## Users and Organization Units

Every user belongs to an **Organization Unit (OU)** and has a **user type**. These two work together:

- The **user type** defines what the user looks like. Their attributes, constraints, and registration rules.
- The **organization unit** defines where the user belongs in the organizational hierarchy.

Each user type belongs to a specific OU. A user of that type can only be created under that OU or one of its descendants. 
This means, for example, you cannot place a Customer-type user under the Default OU, or a Person-type user under the Customers OU.

See [Organization Units](../organization-units/) for more information on how OUs structure your users and [User Types](./user-types) for how user types define attribute schemas and registration settings.

## Onboard a User

<ProductName /> gives you two ways to onboard a user, depending on whether you want to create the account directly or let the user complete their own registration.

<Tabs>
<TabItem value="create" label="Create a User" default>

Use this when you want to create a fully provisioned account on behalf of a user.

1. Navigate to **Users** in the <ProductName /> Console and click **Add User**.
2. Select a **User Type** to determine the user's attribute schema.
3. Select an **Organization Unit** that determines the user's position in the organization hierarchy.
4. Fill in the user attributes.
5. Click **Create User**.

:::tip
The attributes available depend on the selected user type. See [User Type Reference](./user-type-reference) to understand the defaults, or [User Types](./user-types) to create your own.
:::

</TabItem>
<TabItem value="invite" label="Invite a User">

Use this when you want the user to complete their own registration. <ProductName /> generates an invite link that you share with them.

1. Navigate to **Users** and click **Invite User**.
2. Select the **User Type** for the invited user.
3. Enter the user's **Email** address.
4. Click **Next**.
5. Select an invite method:
    - **Send email invite**: <ProductName /> generates the invite and sends the email.
    - **Share invite link manually**: <ProductName /> shows **Invite Link Ready** and a copyable invite link.
6. If the email service is unavailable, <ProductName /> shows **Email service unavailable** and still provides a copyable invite link so you can share it manually.

:::tip
The attributes available on the registration form depend on the selected user type. See [User Type Reference](./user-type-reference) to understand the default user types. See [User Types](./user-types) to create your own user type.
:::

</TabItem>
</Tabs>

## Update a User

1. Open the user from the **Users** list.
2. Edit the attributes you want to change.
3. To update group membership, add or remove groups on the **Groups** tab.
4. Click **Save**.

## Delete a User

1. Open the user from the **Users** list.
2. Click **Delete** and confirm.

:::warning
Deleting a user is permanent. The user's account, attributes, and group memberships are removed immediately.
:::

## Related Guides

- [User Types](./user-types) - Define schemas and attribute rules for each user type
- [Organization Units](../organization-units) - Manage the organizational hierarchy that contains users
- [Flows](../flows/what-is-flows) - Configure registration flows for self-service sign-up
